; This configuration is loaded by .travis.yml and is required as testing APC
; on Travis requires loading the module as well as enabling it for cli usage.

extension=apc.so
apc.enabled=1
apc.enable_cli=1
